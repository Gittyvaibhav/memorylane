# MemoryLane — Frontend

Frontend scaffold for MemoryLane (React + Vite + Tailwind).

Quick start:

```bash
cd memorylane/frontend
npm install
npm run dev
```

The frontend expects an API at `/api` by default. Set `VITE_API_URL` to override.
